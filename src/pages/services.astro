---
import Services from "../components/Services.astro";
import Main from "../layouts/Main.astro";

// const data = await fetch('https://jsonplaceholder.typicode.com/posts');
// const posts = await data.json();
// 👀 note the prerender export
export const prerender = true;

---
<Main title="Services">
  <div class="max-w-screen-md lg:max-w-screen-lg xl:max-w-screen-xl mx-auto">
    <div class="my-10 xl:my-20">
      <h1 class="sub-page-header-highlight"><span class="before:block before:absolute before:-inset-3 before:-skew-y-3 before:bg-pink-500 relative inline-block">
          <span class="relative text-white"> Our Services</span>
          </span>
      </h1>
  </div>
  <Services></Services>
  </div>
    <!-- <h1>Service</h1>
    <ul>
        {posts.slice(0, 6).map((post) => (
            <article class="border-2 p-12 m-2 border-2 border-yellow-600">
              <h3>{post.title}</h3>
              <p set:html={post.body}></p>
              <a href={"../posts/" + post.id }> Read more... </a>
              <hr>
            </article>
          ))}
    </ul> -->
</Main>
